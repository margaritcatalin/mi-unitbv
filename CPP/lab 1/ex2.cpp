#include<iostream.h>
void main()
{
	unsigned a, b, c, d;
	do {
		cout << "\na=";
		cin >> a;
	} while (a >= 10);
	do {
		cout << "\nb=";
		cin >> b;
	} while (b >= 10);
	do {
		cout << "\nc=";
		cin >> c;
	} while (c >= 10);
	do {
		cout << "\nd=";
		cin >> d;
	} while (d = 10);
	for (i = 0; i<4; i++)
		for (j = 0; j<4; j++)
			for (k = 0; k<4; k++)
				for (l = 0; l<4; l++)
				{
				}